let aluno2: { nome: string, aprovado?: boolean, idade: number, notas: [number, number, number, number] }

aluno = {
    nome: "Daniel",
    // aprovado: true,
    idade: 28,
    notas: [1, 2, 3, 4]
}
